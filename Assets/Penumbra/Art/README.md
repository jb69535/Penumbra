# Art

Put Penumbra visual assets here: sprites, tilesets, animations, materials, shaders, lighting textures, and VFX.

Create subfolders when content exists, such as `Characters`, `Environments`, `Tilesets`, `Animations`, `Materials`, and `VFX`.
